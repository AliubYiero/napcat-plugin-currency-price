# CONTEXT — 领域术语表

本文件是领域术语的唯一权威。新术语在概念定型时即写入此处, 与代码冲突时先校对再改。范式体系的引用见 [docs/development-pattern.md](docs/development-pattern.md) 的"领域建模纪律"。

## 术语

### 全局状态单例 (pluginState)

进程内唯一的全局状态对象, 持有 ctx (nullable)、config、data 目录 JSON 读写、logger 与定时器注册表。构造期零 IO, 真正的初始化发生在 `plugin_init(ctx)` 调用其 `init(ctx)` 时。
_Avoid_: "全局变量""context 单例"——它不是裸全局变量, 也不是 ctx 本身。

### 延迟实例化 (lazy instantiation)

store 单例不在模块加载期创建, 而是推迟到业务代码首次调用 `getInstance()` 时。目的: 保证任何文件 IO 都发生在 `plugin_init` 之后。
_Avoid_: "导出即初始化""饿汉单例"——在本框架下属于反模式。

### 指令定义 (InstructionDefinition)

注册表中的一条指令声明: handler + 可选的 `requiredRole` / `scope` / `scopeRules`。分发层在调用 handler 前完成全部校验。
_Avoid_: "命令对象"——不是命令模式, 只是声明式配置。

### 角色推导 (role derivation)

在消息入口处由消息事件一次性推导出发送者角色 (superAdmin / privateUser / admin / user), 而非为每个用户配置权限。"好友私聊即提权"是领域决策: 与机器人互为好友的用户视为可信。
_Avoid_: "权限配置""用户等级"——角色不是配置项, 也没有等级成长概念。

### 会话级开关 (group enabled switch)

群配置中的启用开关, 判定式 `enabled !== false` (宽松判定, 缺失即启用)。默认开启是领域决策, 原因: 插件以零配置可用为目标。
_Avoid_: "默认关闭、手动开启"的表述——与本项目的默认值语义相反。

### 帮助变体 (help variant)

帮助输出的版本档位 (`user` / `admin` / `superAdmin`), 由"角色 + 会话类型"共同决定, 与权限档位不是一一对应 (群聊超管输出 Admin 版)。
_Avoid_: 把"权限档位"直接当"帮助版本"用——两者计数相同但映射不同。

## 业务领域术语

> ⚠️ **与抓取源项目的用词映射**: 抓取源项目 (`qiandao`) 的术语表与文档 (含其 ADR-0001、产品说明、问题解决流程) 把本文的**游戏**称为「**分区**」。本插件统一用「游戏」。引用源项目文档时按此映射理解, 不要在两个词之间来回翻译。

### 游戏 (game)

一个游戏商品专区, 是抓取与订阅的最高层级单位, 如流放之路1、流放之路2、火炬之光。每个游戏有**自己独立的页面、自己的区服清单、自己的通货清单**。用户的订阅与开关都以游戏为粒度。
_Avoid_: 分区、专区、区、服——"分区"是源项目的用词 (见上方映射); 其余要么与之同义混杂, 要么语义不清。

### 区服 (zone)

游戏内的一个具体服务区, 由「区域 / 赛季 / 难度」逐级组合而成, 如 `国服/闪回赛季/普通`。**层数随游戏而变**——流放之路1、流放之路2 是三级, 火炬之光只有两级。**区服不是订阅粒度**: 用户订阅的是游戏, 游戏内的区服组合由配置决定抓哪些。
_Avoid_: 服务器、服务器组合、服。

### 区服标识 (specId)

站点给每个区服分配的内部编号 (如 `3794868`)。价格数据按它归属, 是判断"一份价格属于哪个区服"的**唯一**依据。**配置里不写 specId**——它在运行时从级联接口的选项树中解析得到。
_Avoid_: id、服务器编号、specValues——`specValues` 是另一个接口的字段, 见源项目《问题解决流程》7.4。

### 通货 (currency)

可交易的游戏内物品, 是抓取的最小单位, 如神圣石、崇高石。**同名通货在不同游戏是彼此独立的两条数据**, 不可跨游戏合并。
_Avoid_: 物品、道具、商品。

### 价格记录 (price record)

一次抓取中「某个游戏下的某个区服组合」的全部目标通货价格, **附该组合自身的读取时刻**。游戏与区服组合都是它的定位维度; 读取时刻的粒度是**区服组合**, 不是游戏, 也不是整次抓取。每条价格记录带**比率价**与**元价**两个方向。
_Avoid_: 结果、条目、row。

### 比率价 (ratio price) 与 元价 (rmb price)

同一份价格的两个方向: 比率价是「1 元能买几个」(单位形如 `个/元`), 元价是「每个值多少元」(单位形如 `元/个`)。**两者由站点各自独立给出, 不是精确倒数**——元价只到 4 位小数、比率价只到 3 位, 实测同一时刻 `1 个 = 0.003 元` 与 `1 元 = 314.3911 个` 并存, 取倒数得 333.333, 差 6%。**主方向是比率价**(精度更高), 元价是随它一起存下来的第二个事实。
_Avoid_: 「正价」「反价」「次方向价」——两个方向都如实记录, 只是精度不同; 更**严禁**在任何地方用 `1 / x` 把一方换算成另一方, 那会产出**页面上找不到的数**。

### 挂单 (listing)

详情面板里的一条卖家报价, 含**库存量**与**两个方向的价格**。按面板显示顺序取前 N 条 (N 是常量 5), **不重排**——重排会让"抓到的 top5"与用户看到的 top5 不是同一批。
_Avoid_: 订单、委托、条目。

### 成交量 (volume)

某个通货在本区服**已成交的总量**。原文与解析出的数值**两个都存**：真机上的原文形态只有 `w` 后缀 (`518.3w` / `2w` / `50`) 与空串, 解析器另收 `万`/`W`; 空串是合法值, 表示"详情取到了但成交量没读到"。站点接口不提供这个字段, 只能从页面读——**挂单能走接口, 成交量不能**。
_Avoid_: 交易量、销量、流水。

### 挂单深度 (listing depth)

某个通货在某个区服当前**可售挂单的条数**, 取自详情接口响应的**全量条数** (`data.total`, 缺失时退 `data.items.length`)。
⚠️ **价格项里的 `listings` 是被截断的** (最多 5 条), **不能拿来当深度**——深度用的是与它无关的那个数。
_Avoid_: 挂单数、库存——"库存"是**单条**挂单的可售量 (见「挂单」), 与"一共有几条挂单"是两回事; 也_避免_与「成交量」混用, 那是**已成交**的量。

### 市价失真 (thin market)

挂单深度不足以构成一个价格市场 (**严格少于 15 条**) 时, 接口给出的比率价只是那一小撮挂单的汇总——**它比没有价格更糟, 因为它看起来完全合理**。因此判为失真, **等同于没有价格**: 价格记 `null`, 元价的两个字段一起不出现, **但详情块保留** (失真的是"市场价"这个汇总, 单条挂单的价格仍是事实)。
⚠️ **数不出深度 (响应没到) 与"0 条"是两件事**: 前者**不判定** (不因缺少证据删数据), 后者确定失真。
⚠️ **只降级本来有价的条目**: 本来就没价的条目保持原状。
_Avoid_: 「薄市场」「流动性不足」——失真说的不是"市场冷清"这个描述, 而是"**这个数不可信**"这个判定。

### 详情块 (detail block)

一个通货的**成交量 + 前 N 条挂单**, 挂在价格项上。**三种状态不可归一**: 没开详情 (键不出现) / 开了但没取到 (`null`) / 取到了 (对象)。归一会让"为什么这份数据没有详情"变成无法回答的问题。
_Avoid_: 明细、扩展信息、extra。

### 详情开关 (detail switch)

通货清单里**每个通货各带**的一个布尔值, 决定是否抓它的详情。**默认关闭**: 取详情要为每个通货多做一次点击与一次面板等待, 抓取时长与失败面都随之变大, 应当由用户显式付这个代价。**详情失败不判游戏失败**——它不产生错值 ("没去取"与"取不到"在数据里可区分), 让一个冷门通货的详情超时赔掉整份价格, 收益为零。开关本身进**配置指纹**, 所以开启后旧数据立即判过期, 不必等 5 分钟窗口。
_Avoid_: 把粒度当成游戏或整次抓取的"全局详情开关"。

### 抓取运行 (run)

一次完整的抓取过程 (定时触发或手动触发), 是归档文件中的一条记录。一次运行包含若干游戏的抓取结果, 触发方式 (`schedule` / `manual:{会话键}`) 是其身份的一部分。
_Avoid_: 任务、轮次、批次。

### 数据新鲜度阈值 (staleness threshold)

判定"一个游戏的数据是否还新鲜"的**时间维度**: 窗口是 5 分钟, 依据是 `data.json` 中**该游戏自己的 `readAt`**。判定粒度是**游戏**, 既不是会话也不是全局——手动抓取只重新抓过期的游戏, 新鲜的游戏直接复用现有数据, 然后一并展示。
_Avoid_: "限流""冷却""CD"——它约束的是**数据要不要重新抓**, 不是**用户能不能发指令**。同一个数值承载两种语义, 会导致实现时把"用户被限流"和"数据还新鲜"混为一谈 (设计草稿的 D22/D23 就是这个混淆)。

### 配置指纹 (config fingerprint)

描述"`data.json` 里这份数据是**按哪份配置**抓的"的字符串, 由该游戏的 `pageUrl` / `zoneConfigs` / `currencyList` 序列化而成 (不含游戏名——名字是记录的键, 改名相当于换了个游戏)。**`currencyList` 里的详情开关也算在内**——开了详情的那份数据与没开的那份不是同一份。它是**新鲜度判定的第二个依据**: 指纹与当前配置对不上, 这份数据即判过期, 与 `readAt` 有多新无关。

理由: `zones` 是抓取当时那份配置的**死数据**——组合名、有几个组合、抓了哪些通货都固化在里面。只看 `readAt` 的话, 改完区服组合后 5 分钟内再执行 `price`, 端出来的是旧组合的价格, 且它看起来完全正常。
_Avoid_: "缓存键""哈希"——它不是哈希, 是原样序列化 (顺序敏感: `zoneConfigs` 的顺序就是消息里区服的呈现顺序)。

### 会话 (session) 与 会话键 (session key)

订阅的主体, 是群聊或私聊二者之一; 会话键形如 `group:123456` / `private:789`, 是会话在存储中的唯一标识。
_Avoid_: 群、用户——都会丢掉"私聊也是订阅主体"这层含义。

## 默认值语义声明

- 会话级启用开关: **默认开启** (`enabled !== false`)。语义是"该会话是否响应本插件的指令"。
- **通知开关 (`notifyEnabled`): 默认关闭**。语义是"该会话是否接收主动推送"。默认关闭是领域决策, 原因: 机器人一旦装上就向所有群推送价格是不可接受的, 主动推送必须由用户显式订阅。
- **定时推送小时集合 (`pushHours`): 默认全选** (`0`~`23`, 即每小时)。空数组是**合法语义**, 表示"不做定时推送", 不回退默认 (见 [docs/config-pattern.md](docs/config-pattern.md) 的枚举数组规则)。
- **详情开关 (`detail`): 默认关闭**。语义是"这个通货要不要抓详情"。默认关闭是领域决策, 原因: 每个开了详情的通货都要多花一次点击与一次面板等待, 抓取时长与失败面随之变大; 让用户显式付这个代价, 比"默认全开、嫌慢再一个个关"更符合"零配置可用"——零配置下的默认形态是**有价格、没有盘口**, 这已经是一个能用的答案。
- **市价深度下限 (`MIN_MARKET_LISTINGS`): 常量 15**, 不是配置项。语义是"多少条挂单才构成一个市价"。低于它即判**市价失真** (见上方词条)。不设成配置项的理由与新鲜度阈值同理: 它属于领域语义 ("什么算一个市场"), 与"用户会不会想改"无关。代价是**阈值只来自抓取源项目的实测**, 插件侧的三个游戏未各自验证——因此判定必须**打进日志**, 否则阈值不合适时无从发现。
- **单通货挂单条数上限 (`detailTopN`): 常量 5**, 不是配置项。行数偏多时的收紧方式是**改渲染** (比如只显示最低价那条), 不是改数据形状——抓取端的成本已经付过了。
- **数据新鲜度阈值: 常量 5 分钟**。判定粒度是**游戏** (见上方"数据新鲜度阈值"), 阈值本身不是配置项——它属于领域语义, 与"用户会不会想改"无关。过期判定的**另一个依据是配置指纹** (见上方"配置指纹"), 它由配置本身决定, 没有可调的数值。
- 指令前缀: **固定的 `#currency`**, **不是用户配置项**。它被烧进帮助图片与文本映射, 运行期可改就会出现"帮助图上的指令敲不出来"; 只能由开发者改源码 (`DEFAULT_CONFIG.commandPrefix`) 并**同步重跑帮助生成** (见 [docs/design.md](docs/design.md) §5.1 §19)。
- 只有前缀、无参数 (如 `#currency` 单独出现) 时, 路由到帮助指令 (见 [docs/instruction-pattern.md](docs/instruction-pattern.md))。

新增有默认值语义的配置项时, 在此处声明选择及理由; 难逆转的补 ADR (判据见 [docs/development-pattern.md](docs/development-pattern.md) 的"ADR 纪律")。
